# UI/UX 디자인 원칙

> 좋은 UI는 단순히 예쁜 화면을 만드는 것이 아니라,  
> 사용자가 **무엇을 먼저 보고, 어떻게 이해하며, 자연스럽게 다음 행동으로 이동하는지​**를 설계하는 과정이다.

---

## 1. 계층 구조 (Visual Hierarchy)

### 핵심 원칙
사용자가 화면에서 **중요한 정보와 덜 중요한 정보를 즉시 구분**할 수 있도록 시각적인 우선순위를 설계한다.

디자인은 책의 구조와 유사하다. 제목, 장, 섹션이 존재하듯 UI 역시 사용자가 현재 위치와 중요한 정보를 쉽게 파악할 수 있어야 한다.

### 주요 방법

#### ① 글꼴 크기 및 두께
- 크고 굵은 텍스트 → 가장 중요한 정보
- 중간 크기의 텍스트 → 섹션 제목
- 작은 텍스트 → 보조 설명

#### ② 대비
- 중요한 CTA와 핵심 정보에 높은 대비 적용
- 보조 정보는 상대적으로 낮은 대비 적용

#### ③ 간격
- 관련 있는 요소는 가까이 배치
- 서로 다른 그룹은 충분한 여백으로 구분
- 여백을 통해 정보의 덩어리와 우선순위 형성

### 디자인 체크포인트
- 사용자가 화면을 처음 봤을 때 가장 먼저 무엇을 보는가?
- 중요한 정보가 스크롤 없이 보이는가?
- 정보의 중요도에 따라 명확한 크기 차이가 존재하는가?

> **핵심:** 사용자의 시선 흐름을 의도적으로 설계한다.

---

## 2. 점진적 공개 (Progressive Disclosure)

### 핵심 원칙
사용자에게 모든 정보를 한 번에 보여주지 않고, **현재 단계에서 필요한 정보만 제공**한다.

기능과 정보가 많다고 해서 모두 한 화면에 배치하면 사용자는 부담을 느끼고 행동을 포기할 수 있다.

### 적용 방법

복잡한 프로세스를 여러 단계로 나눈다.

```
정보 입력
   ↓
선택
   ↓
확인
   ↓
완료
```

각 단계에서는 사용자가 하나의 명확한 행동에 집중할 수 있도록 설계한다.

### 예시

❌ 한 화면에 모든 회원가입 정보 입력

```
이름
이메일
전화번호
직업
관심 분야
주소
추가 정보
...
```

⭕ 단계별 정보 입력

```
STEP 1. 기본 정보
↓
STEP 2. 관심 분야
↓
STEP 3. 설정 완료
```

### 디자인 체크포인트
- 한 화면에 너무 많은 선택지가 존재하지 않는가?
- 현재 사용자가 해야 할 행동이 명확한가?
- 전체 과정 중 현재 위치를 알 수 있는가?
- 남은 단계가 얼마나 되는지 알 수 있는가?

> **핵심:** 복잡한 경험을 작은 단위로 나누어 인지 부담을 줄인다.

---

## 3. 일관성 (Consistency)

### 핵심 원칙
사용자가 새로운 화면에서도 **이전에 학습한 인터페이스 패턴을 그대로 활용할 수 있어야 한다.**

버튼, 컬러, 아이콘, 간격, 인터랙션 방식 등이 화면마다 달라지면 사용자는 매번 새로운 규칙을 학습해야 한다.

### 일관성을 유지해야 하는 요소

- 버튼 스타일
- 컬러 시스템
- Typography
- 아이콘 스타일
- 카드 디자인
- 여백 규칙
- 화면 구조
- 인터랙션 방식

### 예시

같은 역할의 버튼이라면 항상 동일한 스타일을 사용한다.

```
Primary Button
→ 주요 행동

Secondary Button
→ 보조 행동

Text Button
→ 낮은 중요도의 행동
```

갑자기 특정 화면에서만 버튼의 크기나 색상이 달라지면 사용자의 인지 부담이 증가한다.

### 디자인 체크포인트

- 같은 기능은 항상 같은 UI 패턴을 사용하는가?
- 버튼의 의미가 화면마다 바뀌지 않는가?
- 컬러의 역할이 일관적인가?
- 새로운 화면에서도 사용자가 사용법을 예상할 수 있는가?

> **핵심:** 사용자가 인터페이스를 다시 학습하지 않도록 한다.

---

## 4. 대비 (Contrast)

### 핵심 원칙
중요한 콘텐츠와 행동에 사용자의 시선을 집중시키기 위해 **색상, 크기, 명도 등의 차이​**를 활용한다.

대비는 단순히 색상을 다르게 사용하는 것이 아니라 **정보의 중요도를 시각적으로 표현하는 도구**다.

### 대비를 만들 수 있는 요소

- 색상
- 크기
- 굵기
- 명도
- 형태
- 위치
- 여백

### 예시

```
[ 계정 삭제 ]  ← 높은 대비 / 위험 행동 강조

계정 유지       ← 낮은 대비 / 보조 행동
```

### 적용 원칙

| 중요도 | 대비 수준 |
|---|---|
| 핵심 행동 | 높음 |
| 주요 정보 | 중간 |
| 보조 정보 | 낮음 |
| 비활성 요소 | 매우 낮음 |

### 디자인 체크포인트

- 가장 중요한 CTA가 즉시 보이는가?
- 모든 요소가 강조되어 오히려 복잡하지 않은가?
- 강조와 비강조의 차이가 명확한가?

> **핵심:** 모든 것을 강조하면 아무것도 강조되지 않는다.

---

## 5. 접근성 (Accessibility)

### 핵심 원칙
특정 사용자만이 아니라 **모든 사용자가 서비스를 이용할 수 있도록 설계**한다.

색각 이상, 시각 장애, 운동 능력의 차이 등 다양한 사용 환경을 고려해야 한다.

### 주요 접근성 원칙

#### ① 충분한 색상 대비
텍스트와 배경 사이에 충분한 명도 차이를 확보한다.

```
좋은 예
검정 텍스트 + 밝은 배경

주의
연한 회색 텍스트 + 흰색 배경
```

#### ② 색상만으로 정보 전달하지 않기

❌

```
빨간색 = 오류
초록색 = 성공
```

⭕

```
⚠ 오류 발생
✓ 완료되었습니다
```

색상과 함께 아이콘 또는 텍스트를 제공한다.

#### ③ 충분한 터치 영역

모바일에서는 버튼과 아이콘 사이에 충분한 간격과 터치 영역을 확보한다.

#### ④ 명확한 텍스트

아이콘만 사용하는 것보다 필요한 경우 명확한 레이블을 함께 제공한다.

### 접근성 체크리스트

- [ ] 텍스트와 배경의 대비가 충분한가?
- [ ] 색상만으로 정보를 전달하지 않는가?
- [ ] 이미지에 대체 텍스트를 제공할 수 있는가?
- [ ] 키보드만으로 탐색 가능한가?
- [ ] 터치 영역이 충분한가?
- [ ] 보조 기술과 호환되는 구조인가?

> **핵심:** 좋은 디자인은 더 많은 사람이 사용할 수 있는 디자인이다.

---

## 6. 근접성 (Proximity)

### 핵심 원칙
서로 관련 있는 요소는 **가까이 배치하고**, 관련 없는 요소는 충분히 분리한다.

사용자는 물리적으로 가까운 요소들을 자연스럽게 하나의 그룹으로 인식한다.

### 예시

⭕ 관련 요소

```
상품 이미지
상품 이름
상품 가격
[ 구매하기 ]
```

위 요소들은 하나의 상품 정보이므로 가까운 영역에 배치한다.

반면,

```
상품 정보


────────────


서비스 설정
```

서로 다른 정보 그룹은 충분한 여백으로 분리한다.

### 적용 방법

- 같은 기능 → 가까이 배치
- 다른 기능 → 여백으로 분리
- 관련 버튼 → 하나의 그룹
- 위험 행동 → 일반 행동과 분리

### 디자인 체크포인트

- 사용자가 어떤 요소들이 서로 관련 있는지 즉시 알 수 있는가?
- 서로 다른 정보 그룹 사이에 충분한 여백이 있는가?
- 버튼의 위치가 잘못된 행동을 유도하지 않는가?

> **핵심:** 거리 자체가 정보의 관계를 설명한다.

---

## 7. 정렬 (Alignment)

### 핵심 원칙
모든 UI 요소는 보이지 않는 **그리드와 정렬 기준** 위에서 배치되어야 한다.

정렬은 디자인에 질서와 안정감을 제공하며 사용자의 탐색을 쉽게 만든다.

### 좋은 정렬의 특징

- 동일한 시작선
- 일정한 여백
- 반복되는 간격
- 명확한 그리드
- 예측 가능한 배치

### 예시

❌ 불규칙한 정렬

```
제목
    설명 텍스트
버튼
       카드
```

⭕ 일관된 정렬

```
제목
설명 텍스트
[ 버튼 ]

카드
카드
카드
```

### 디자인 체크포인트

- 텍스트의 시작점이 정렬되어 있는가?
- 카드와 버튼의 간격이 일정한가?
- 화면마다 동일한 그리드 시스템을 사용하는가?
- 요소가 임의의 위치에 배치되지 않았는가?

> **핵심:** 보이지 않는 선이 화면 전체의 질서를 만든다.

---

# 효과적인 UI 디자인을 위한 실전 원칙

## 1. 사용자 관점에서 설계한다

디자이너가 보여주고 싶은 순서가 아니라 **사용자가 목표를 달성하는 순서**로 정보를 배치한다.

```
사용자 목표
↓
필요한 정보 확인
↓
선택
↓
행동
↓
결과 확인
```

UI는 사용자의 사고 흐름을 따라야 한다.

---

## 2. 가능한 한 간단하게 만든다

좋은 인터페이스는 사용자가 인터페이스 자체를 의식하지 않도록 만든다.

### 단순화를 위한 질문

- 이 기능이 정말 필요한가?
- 이 정보를 지금 보여줘야 하는가?
- 두 단계를 한 단계로 줄일 수 있는가?
- 사용자가 직접 입력하지 않아도 되는 정보는 없는가?

> **좋은 UI는 기능이 적은 것이 아니라, 사용자가 복잡함을 느끼지 않는 것이다.**

---

## 3. 빠른 행동을 위한 단축 기능을 제공한다

반복적인 행동은 가능한 한 빠르게 수행할 수 있어야 한다.

### 예시

- 최근 검색어
- 자동완성
- 즐겨찾기
- 빠른 선택 버튼
- 키보드 단축키
- 이전 입력값 자동 적용

사용 빈도가 높은 행동일수록 더 적은 단계로 접근할 수 있도록 설계한다.

---

## 4. 반드시 테스트한다

디자이너가 예상한 사용자 행동과 실제 사용자의 행동은 다를 수 있다.

따라서 실제 사용자가 화면을 사용하는 모습을 관찰해야 한다.

### 테스트 시 확인할 요소

- 사용자가 첫 화면에서 무엇을 보는가?
- CTA를 쉽게 발견하는가?
- 예상하지 못한 행동을 하는가?
- 특정 단계에서 멈추는가?
- 설명 없이 기능을 이해하는가?

> **디자인의 완성은 디자이너의 판단이 아니라 사용자의 행동으로 검증된다.**

---

# UI 디자인 최종 체크리스트

## 정보 구조

- [ ] 가장 중요한 정보가 가장 먼저 보이는가?
- [ ] 정보의 우선순위가 명확한가?
- [ ] 제목 → 설명 → 행동의 흐름이 자연스러운가?

## 복잡성

- [ ] 한 화면에 너무 많은 정보를 제공하지 않는가?
- [ ] 복잡한 과정은 단계별로 나누었는가?
- [ ] 사용자가 현재 진행 상황을 알 수 있는가?

## 일관성

- [ ] 버튼 스타일이 일관적인가?
- [ ] 컬러 시스템이 일관적인가?
- [ ] Typography 규칙이 유지되는가?
- [ ] 화면 구조가 예측 가능한가?

## 시각적 설계

- [ ] 중요한 요소가 충분히 강조되는가?
- [ ] 관련 요소는 가까이 배치되어 있는가?
- [ ] 정렬과 간격이 일관적인가?
- [ ] 불필요한 시각 요소가 없는가?

## 접근성

- [ ] 충분한 색상 대비가 확보되었는가?
- [ ] 색상만으로 정보를 전달하지 않는가?
- [ ] 작은 텍스트가 너무 많지 않은가?
- [ ] 버튼과 터치 영역이 충분한가?

---

# 핵심 디자인 철학

> **사용자가 생각하지 않아도 이해할 수 있어야 한다.**

좋은 UI는 화려한 디자인보다 먼저 다음 질문에 답해야 한다.

1. 사용자는 무엇을 가장 먼저 봐야 하는가?
2. 사용자는 지금 무엇을 해야 하는가?
3. 다음 행동은 무엇인가?
4. 이 화면을 처음 보는 사람도 이해할 수 있는가?

디자인의 목적은 화면을 채우는 것이 아니라,  
**정보를 정리하고 사용자의 행동을 자연스럽게 안내하는 것**이다.