# 🎨 UI/UX 디자인 스킬 & 시스템 가이드라인 (Design Skill Guide)

> 본 문서는 레퍼런스 이미지(Air Premia 모바일 앱 레이아웃 및 iM 브랜드 컬러 팔레트)의 시각적 패턴 분석 결과와 [`UI-UX 디자인 원칙.md`](file:///c:/Users/dusro/OneDrive/Desktop/iM-Goal2/UI-UX%20%EB%94%94%EC%9E%90%EC%9D%B8%20%EC%9B%90%EC%B9%99.md)를 결합하여 작성된 **디자인 규칙, 60-30-10 컬러 배분 스킬 및 CSS 변수 시스템 가이드**입니다.

---

## 1. CSS 변수 시스템 (`:root` 디자인 토큰)

브랜드 고유 팔레트와 **60-30-10 법칙(주요-보조-강조)**을 적용한 컬러 토큰, 그리고 **폰트 크기, 자간, 행간, 여백(Spacing), 코너 라운딩(Border Radius), 컴포넌트 규격(Component Sizing)**을 체계적으로 정의한 CSS 변수 모음입니다.

```css
:root {
  /* ==========================================================================
     1. Brand Color Palette (iM 고유 브랜드 색상 정의)
     ========================================================================== */
  /* Primary Colors */
  --im-mint:     #00C7A9; /* RGB(0, 199, 169) / Pantone 326C */
  --im-lime:     #E2F15E; /* RGB(226, 241, 94) / Pantone 585C */
  --im-gradient: linear-gradient(90deg, #00C7A9 0%, #E2F15E 100%);

  /* Secondary Colors */
  --im-blue:       #7DB5FF; /* RGB(125, 181, 255) / Pantone 292C */
  --im-purple:     #D1B5FF; /* RGB(209, 181, 255) / Pantone 264C */
  --im-light-blue: #53E1E5; /* RGB(83, 225, 229) / Pantone 3105C */
  --im-beige:      #CDC9B7; /* RGB(205, 201, 183) / Pantone 7528C */

  /* Neutral / Logotype Color */
  --im-gray:       #666666; /* RGB(102, 102, 102) / Pantone Cool Gray 10C */
  --im-gray-dark:  #1F2937; /* RGB(31, 41, 55) - 메인 본문 텍스트용 */
  --im-white:      #FFFFFF;
  --im-bg-canvas:  #F8FAF9; /* 시각적 피로도를 줄여주는 미세 민트 틴트 베이스 */


  /* ==========================================================================
     2. 60 - 30 - 10 Color Rule Tokens (시각적 비중 배분 토큰)
     ========================================================================== */
  
  /* 🟢 [60%] Dominant / Main Background & Base Structure (주요 영역 60%)
     - 화면의 60%를 차지하는 캔버스, 전체 배경, 본문 기본 텍스트 및 기본 구조 line */
  --color-60-bg-base:        var(--im-bg-canvas);   /* 전체 화면 배경 */
  --color-60-bg-surface:     var(--im-white);       /* 메인 컨텐츠 배경 */
  --color-60-text-primary:   var(--im-gray-dark);   /* 메인 텍스트 (명도 대비 확보) */
  --color-60-text-secondary: var(--im-gray);        /* 보조 설명 텍스트 */
  --color-60-border:         #E5E7EB;               /* 기본 구분선 */

  /* 🔵 [30%] Secondary / Structural Cards, Tabs & Sub Surfaces (보조 영역 30%)
     - 화면의 30%를 차지하는 카드 배경, 토글 탭, 필터 칩, 보조 버튼, 카테고리 뱃지 */
  --color-30-surface-sub:    #F0F7F6;               /* 민트 틴트 서브 카드 배경 */
  --color-30-card-beige:     var(--im-beige);      /* 보조 필터 / 카테고리 배경 */
  --color-30-chip-blue:      var(--im-blue);       /* 정보 알림 / 서브 칩 */
  --color-30-chip-purple:    var(--im-purple);     /* 이벤트 / 카테고리 칩 */
  --color-30-chip-cyan:      var(--im-light-blue); /* 수신 메시지 / 보조 태그 */
  --color-30-tab-bg:         #EBF2F1;               /* 세그먼트 탭 배경 */
  --color-30-text-subtle:    #4B5563;               /* 서브 카테고리 텍스트 */

  /* ⚡ [10%] Accent / Highlight & Primary CTAs (강조 영역 10%)
     - 화면의 10%만 제한적으로 사용하는 시선 집중 포인트: Primary CTA, D-Day 배지, 활성 탭 */
  --color-10-accent-primary: var(--im-mint);       /* 메인 액션 버튼 (CTA) */
  --color-10-accent-sub:     var(--im-lime);       /* D-Day / 주의 / 포인트 강조 배지 */
  --color-10-accent-gradient:var(--im-gradient);   /* 히어로 비주얼 / 강조 CTA 그래디언트 */
  --color-10-text-on-accent: var(--im-gray-dark);   /* Lime/Mint 위 가독성 높은 텍스트 */
  --color-10-text-on-mint:   #FFFFFF;               /* Mint 위 흰색 텍스트 */


  /* ==========================================================================
     3. Typography System (타이포그래피 스케일)
     ========================================================================== */
  --font-family-sans: -apple-system, BlinkMacSystemFont, "Pretendard", "Segoe UI", Roboto, sans-serif;

  /* Font Sizes */
  --font-size-3xl: 1.75rem;  /* 28px - 디스플레이/대형 코드 (ICN, SFO, HNL) */
  --font-size-2xl: 1.5rem;   /* 24px - 메인 헤더 / 대형 액션 타이틀 */
  --font-size-xl:  1.25rem;  /* 20px - 화면 타이틀 / 모달 헤더 */
  --font-size-lg:  1.125rem; /* 18px - 카드 섹션 제목 / 강조 타이틀 */
  --font-size-md:  1rem;     /* 16px - 기본 본문 / 주요 버튼 텍스트 */
  --font-size-sm:  0.875rem; /* 14px - 보조 설명 / 입력 폼 레이블 / 탭 메뉴 */
  --font-size-xs:  0.8125rem;/* 13px - 캡션 / 상세 메타데이터 값 */
  --font-size-2xs: 0.75rem;  /* 12px - 서브 메타데이터 레이블 / D-Day 태그 */

  /* Font Weights */
  --font-weight-regular: 400;
  --font-weight-medium:  500;
  --font-weight-semibold: 600;
  --font-weight-bold:    700;

  /* Line Heights */
  --line-height-tight:  1.25;  /* 대형 코드 및 헤드라인용 */
  --line-height-snug:   1.35;  /* 카드의 주요 정보용 */
  --line-height-normal: 1.5;   /* 기본 본문 텍스트용 */
  --line-height-relaxed:1.65;  /* 가독성이 필요한 안내문용 */


  /* ==========================================================================
     4. Spacing & Layout System (여백 및 레이아웃 스케일 - 4/8pt Grid)
     ========================================================================== */
  --space-0-5: 0.25rem; /* 4px */
  --space-1:   0.5rem;  /* 8px */
  --space-1-5: 0.75rem; /* 12px */
  --space-2:   1rem;    /* 16px */
  --space-2-5: 1.25rem; /* 20px */
  --space-3:   1.5rem;  /* 24px */
  --space-4:   2rem;    /* 32px */

  --screen-padding-x: var(--space-2-5); /* 모바일 화면 좌우 여백 (20px) */
  --screen-padding-y: var(--space-2);   /* 모바일 화면 상하 여백 (16px) */
  --card-padding-x:   var(--space-2-5); /* 카드 내부 좌우 여백 (20px) */
  --card-padding-y:   var(--space-2-5); /* 카드 내부 상하 여백 (20px) */
  --section-gap:      var(--space-3);   /* 섹션 간 간격 (24px) */
  --component-gap:    var(--space-1-5); /* 카드 내 요소 간 간격 (12px) */


  /* ==========================================================================
     5. Border Radius System (라운딩 규칙)
     ========================================================================== */
  --radius-xs:   0.25rem; /* 4px  - 배지 / 미니 칩 */
  --radius-sm:   0.5rem;  /* 8px  - 입력창 / 서브 칩 */
  --radius-md:   0.75rem; /* 12px - 서브 버튼 */
  --radius-lg:   1rem;    /* 16px - 주요 버튼 / 표준 카드 */
  --radius-xl:   1.25rem; /* 20px - 탑승권 카드 / 메인 카테고리 */
  --radius-2xl:  1.5rem;  /* 24px - 바텀시트 상단 / 모달 */
  --radius-pill: 9999px;  /* 알약 형태 탭 & 태그 */


  /* ==========================================================================
     6. Component Dimension System (컴포넌트 규격 표준)
     ========================================================================== */
  --touch-target-min: 2.75rem; /* 44px (WCAG 접근성 기준) */
  --header-height:       3.25rem; /* 52px */
  --bottom-nav-height:   3.75rem; /* 60px */
  --btn-height-xl: 3.5rem;   /* 56px - 하단 고정 Primary CTA 버튼 */
  --btn-height-lg: 3.25rem;  /* 52px - 표준 카드 내 CTA 버튼 */
  --btn-height-sm: 2.25rem;  /* 36px - 소형 탭 / 칩 버튼 */


  /* ==========================================================================
     7. Elevation & Motion System (그림자 및 트랜지션)
     ========================================================================== */
  --shadow-subtle: 0 2px 8px rgba(0, 0, 0, 0.04);
  --shadow-card:   0 4px 16px rgba(0, 199, 169, 0.08); /* 브랜드 민트 틴트 섀도우 */
  --shadow-float:  0 8px 24px rgba(0, 0, 0, 0.12);

  --transition-fast: all 150ms cubic-bezier(0.33, 1, 0.68, 1);
  --transition-normal: all 250ms cubic-bezier(0.4, 0, 0.2, 1);
}
```

---

## 2. 60 - 30 - 10 컬러 배분 규칙 (Color Proportion Rule)

시각적 균형과 브랜드 인지도를 극대화하기 위해 UI 화면 전체 시각 영역을 **60 : 30 : 10** 비율로 분할하여 사용합니다.

```
┌─────────────────────────────────────────────────────────────┐
│ 🟢 60% Dominant (화면 캔버스, 여백, 기본 텍스트)               │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ 🔵 30% Secondary (카드, 서브 영역, 탭, 보조 칩)             │ │
│ │ ┌─────────────────────────────────────────────────────┐ │ │
│ │ │ ⚡ 10% Accent (Primary CTA, D-Day 배지, 핵심 하이라이트)│ │ │
│ │ └─────────────────────────────────────────────────────┘ │ │
│ └─────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

### 🟢 60% - 주 배경 & 캔버스 (Dominant Base)
* **목적**: 화면 전체의 눈의 피로감을 줄이고 깔끔한 가독성을 제공하는 백그라운드 영역.
* **적용 요소**: 전체 화면 배경 (`--color-60-bg-base`), 메인 카드 백그라운드 (`--color-60-bg-surface`), 본문 타이포그래피 (`--color-60-text-primary`), 보조 텍스트 (`--color-60-text-secondary`).
* **색상**: 소프트 화이트 (`#FFFFFF`), 베이스 캔버스 틴트 (`#F8FAF9`), 텍스트용 다크 그레이 (`#1F2937`), 보조 그레이 (`--im-gray`).

### 🔵 30% - 보조 카드 & 서브 요소 (Secondary Structure)
* **목적**: 정보의 덩어리를 시각적으로 묶어주고 카테고리와 구성을 체계화하는 영역.
* **적용 요소**: 여정 카드, 정보 영역 서브 배경, 세그먼트 알약 탭 배경, 카테고리 칩 (`iM Blue`, `iM Purple`, `iM Light Blue`, `iM Beige`).
* **색상**: 
  - `iM Blue` (`#7DB5FF`): 메인 서브 액션 칩 / 정보 카드 테두리
  - `iM Purple` (`#D1B5FF`): 이벤트 & 특별 카테고리 배지
  - `iM Light Blue` (`#53E1E5`): 수신 알림 태그 / 서브 하이라이트
  - `iM Beige` (`#CDC9B7`): 중립 서브 카테고리 필터 배경

### ⚡ 10% - 강조 포인트 & 액션 (Accent Highlight)
* **목적**: 사용자가 고민 없이 즉시 다음 행동을 수행할 수 있도록 **시선을 단번에 사로잡는 시각적 가이드**.
* **규칙**: 전체 화면의 10% 이상 남발하지 않으며, 핵심 타겟에만 제한적으로 사용.
* **적용 요소**: 하단 고정 Primary CTA 버튼, D-Day 상태 강조 배지, 활성 탭 표시, 메인 차트/프로그레스 포인트.
* **색상**:
  - `iM Mint` (`#00C7A9`): Primary CTA 버튼, 활성 탭 인디케이터
  - `iM Lime` (`#E2F15E`): High-visibility D-Day 뱃지, 중요 경고/상태 칩
  - `iM Gradient` (`linear-gradient(90deg, #00C7A9, #E2F15E)`): 대표 브랜드 헤더, 주요 전환 버튼

---

## 3. 레퍼런스 이미지 기반 UI/UX 패턴 분석 & 디자인 원칙 연결

Air Premia 모바일 앱의 레이아웃 패턴과 `UI-UX 디자인 원칙.md` 및 **60-30-10 컬러 배분 규칙**의 통합 연계 가이드입니다.

### ① 시각적 계층 구조 (Visual Hierarchy) & 10% 강조
* **원칙 적용**: 가장 중요한 정보와 행동(공항 코드, CTA 버튼)에 10% Accent Color와 최대 Typography Contrast를 적용.
* **스타일 규칙**:
  - **Hero Text (공항 코드 / 상태)**: `font-size: var(--font-size-3xl)` (28px) + `font-weight: var(--font-weight-bold)`.
  - **Primary CTA 버튼**: `background: var(--color-10-accent-primary)` (iM Mint) 또는 `var(--color-10-accent-gradient)` (iM Gradient) 적용.

### ② 카드 & 근접성 (Proximity) & 30% 보조 영역
* **원칙 적용**: 서로 다른 정보 그룹은 30% 보조 컬러 카드 및 라운딩으로 묶어 직관적으로 분리.
* **스타일 규칙**:
  - **메인 카드는 60% 캔버스 위 30% 서브 컬러 배치**: `background: var(--color-60-bg-surface)`, `border-radius: var(--radius-xl)` (20px).
  - **메타데이터 칩 & 태그**: `background: var(--color-30-chip-blue)` 또는 `var(--color-30-surface-sub)` 적용.

### ③ 점진적 공개 (Progressive Disclosure) & 접근성
* **원칙 적용**: 버튼과 탭의 터치 영역은 최소 44px(`--touch-target-min`) 이상을 유지하고, Lime/Mint 배지 위 텍스트 명도 대비(Contrast Ratio 4.5:1) 확보.

---

## 4. 실전 60-30-10 적용 CSS 컴포넌트 스니펫

### 1) 60-30-10 원칙이 적용된 메인 티켓 카드 (`.im-ticket-card`)
```css
/* 60% 영역: 화이트 캔버스 카드 배경 + 명확한 텍스트 명도 */
.im-ticket-card {
  background-color: var(--color-60-bg-surface);
  border-radius: var(--radius-xl);
  padding: var(--card-padding-y) var(--card-padding-x);
  box-shadow: var(--shadow-card);
  border: 1px solid var(--color-60-border);
  display: flex;
  flex-direction: column;
  gap: var(--component-gap);
}

/* 10% 영역: 시선을 사로잡는 D-Day Lime 강조 배지 */
.im-ticket-card__badge-dday {
  display: inline-flex;
  align-items: center;
  padding: var(--space-0-5) var(--space-1-5);
  background-color: var(--color-10-accent-sub); /* iM Lime (#E2F15E) */
  color: var(--color-10-text-on-accent);         /* 가독성 높은 다크 그레이 */
  font-size: var(--font-size-2xs);
  font-weight: var(--font-weight-bold);
  border-radius: var(--radius-pill);
}

/* 60% 영역: 대형 공항 코드 히어로 타이포그래피 */
.im-ticket-card__code {
  font-size: var(--font-size-3xl);
  font-weight: var(--font-weight-bold);
  color: var(--color-60-text-primary);
  line-height: var(--line-height-tight);
}

/* 30% 영역: 3열 메타데이터 그리드 구분선 및 서브 레이블 */
.im-ticket-card__meta-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: var(--space-2);
  padding-top: var(--space-2);
  border-top: 1px dashed #D1D5DB;
  background-color: var(--color-30-surface-sub); /* 민트 틴트 서브 카운터 */
  padding: var(--space-2);
  border-radius: var(--radius-lg);
}

.im-ticket-card__meta-label {
  font-size: var(--font-size-2xs);
  color: var(--color-60-text-secondary);
}

.im-ticket-card__meta-value {
  font-size: var(--font-size-md);
  font-weight: var(--font-weight-bold);
  color: var(--color-60-text-primary);
}
```

### 2) 10% Accent가 적용된 Primary CTA 버튼 (`.im-btn-primary`)
```css
/* 10% 영역: 브랜드 신뢰감을 유도하는 iM Mint 메인 CTA */
.im-btn-primary {
  width: 100%;
  height: var(--btn-height-xl);
  background-color: var(--color-10-accent-primary); /* iM Mint (#00C7A9) */
  color: var(--color-10-text-on-mint);              /* 화이트 (#FFFFFF) */
  font-size: var(--font-size-md);
  font-weight: var(--font-weight-bold);
  border-radius: var(--radius-lg);
  border: none;
  cursor: pointer;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  transition: var(--transition-fast);
}

.im-btn-primary:hover {
  filter: brightness(0.95);
}

.im-btn-primary:active {
  transform: scale(0.98);
}

/* 시그니처 그래디언트 액션 버튼 변형 */
.im-btn-gradient {
  background: var(--color-10-accent-gradient); /* Mint ~ Lime Gradient */
  color: var(--color-10-text-on-accent);
}
```

### 3) 30% Secondary 세그먼트 알약 탭 (`.im-segmented-tab`)
```css
/* 30% 영역: 서브 컨트롤 영역 배경 */
.im-segmented-tab {
  display: inline-flex;
  background-color: var(--color-30-tab-bg);
  padding: var(--space-0-5);
  border-radius: var(--radius-pill);
  gap: var(--space-0-5);
}

.im-segmented-tab__item {
  height: var(--btn-height-sm);
  padding: 0 var(--space-2);
  border-radius: var(--radius-pill);
  font-size: var(--font-size-sm);
  font-weight: var(--font-weight-medium);
  color: var(--color-30-text-subtle);
  border: none;
  background: transparent;
  cursor: pointer;
  transition: var(--transition-fast);
}

/* 10% 활성화 선택 영역 */
.im-segmented-tab__item--active {
  background-color: var(--color-60-bg-surface);
  color: var(--color-60-text-primary);
  font-weight: var(--font-weight-bold);
  box-shadow: var(--shadow-subtle);
  border: 1px solid var(--color-10-accent-primary);
}
```

---

## 5. UI 검수 체크리스트 (60-30-10 & 디자인 원칙)

- [ ] **60% 배경 비중**: 전체 화면 영역 중 60% 이상이 편안한 캔버스(`--color-60-bg-base`) 및 여백인가?
- [ ] **30% 구조 비중**: 카드, 세그먼트 탭, 카테고리 칩이 30% 범위 내에서 체계적으로 배치되었는가?
- [ ] **10% 강조 절제**: iM Mint / Lime / Gradient 강조 색상이 무분별하게 남발되지 않고, Primary CTA 및 D-Day 배지 등 핵심 요소에만 10% 비율로 적용되었는가?
- [ ] **텍스트 명도 대비 (WCAG AA)**: iM Lime(`--im-lime`) 배경 위 텍스트는 밝은 색이 아닌 다크 그레이(`--im-gray-dark`)를 사용하여 최소 4.5:1 이상 대비를 확보했는가?
- [ ] **타이포그래피 및 여백**: 폰트 규격(`--font-size-*`)과 8pt 그리드(`--space-*`) 및 모서리 라운딩(`--radius-*`) 변수를 사용했는가?
